services:
  fridgepal-app-frontend:
    container_name: fridgepal-app-frontend
    image: fridgepal-app-frontend
    build:
      context: .
      dockerfile: ./Dockerfile
      args:
        VITE_API_URL: http://localhost:3000/api
    ports:
      - '80:80'
